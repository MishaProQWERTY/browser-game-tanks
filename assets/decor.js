module.exports.forest = function forest () { return 1 }

module.exports.update = function update(world) {
    for (let x = 0; x < world.date.decor.length; x++) {
        for (let y = 0; y < world.date.decor[x].length; y++) {
            let decor = world.date.decor[x][y]
            if (decor == 1) {
                
            }
        }
    }
}