module.exports.dosok = function dosok () { return 1 }
module.exports.foliage = function foliage () { return 2 }
module.exports.stone = function stone () { return 3 }

module.exports.update = function update(world) {
    for (let x = 0; x < world.date.blocks.length; x++) {
        for (let y = 0; y < world.date.blocks[x].length; y++) {
            
        }
    }
}